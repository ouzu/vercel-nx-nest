{
  "version": 2,
  "installCommand": "cd .. && npm ci",
  "buildCommand": "cd .. && npx prisma generate && npx nx build app",
  "outputDirectory": "../dist/app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
