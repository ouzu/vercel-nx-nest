{
  "version": 2,
  "installCommand": "cd .. && npm ci",
  "buildCommand": "cd .. && npx nx build app",
  "outputDirectory": "../dist/app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
